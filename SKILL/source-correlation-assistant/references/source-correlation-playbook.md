# Source Correlation Playbook

## 核心目标

将“日志异常”转化为“代码定位线索”，支持开发快速修复。

## 关键流程

1. 从日志中提取 `文件名:行号` 模式。
2. 在 `source_root` 建立文件名索引。
3. 命中后回填源码片段（上下文 2~3 行）。
4. 输出“日志行号 -> 源码文件:行号”映射表。

## 注意事项

- 同名文件多路径时，先命中首个，再提示可能冲突。
- 未命中源码时，要显式说明“日志存在定位线索，但当前源码目录未命中”。
- 证据片段必须脱敏后展示。
